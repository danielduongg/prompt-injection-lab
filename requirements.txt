# Core (required for the offline, reproducible demo)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Optional backends for evaluating REAL models (not needed for the offline demo).
# Uncomment what you need and supply API keys via environment variables.
# anthropic>=0.39          # ANTHROPIC_API_KEY
# openai>=1.40             # OPENAI_API_KEY
# transformers>=4.44       # local open-source models
# torch>=2.2
# datasets>=2.20           # to load deepset/prompt-injections, JailbreakBench, etc.
